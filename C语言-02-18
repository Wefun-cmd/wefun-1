#include<stdio.h>
int main()
{
	//int ch = getchar();//只能处理一个字符
	//ch = ch + 32;
	//putchar(ch);
	int ch = 0;
	while ((ch = getchar()) != EOF)
	{
		printf("%c\n", ch + 32);
		getchar();
	}
	return 0;
}
