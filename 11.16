int main()
{
	/*int a = 10;
	int* pa = &a;
	char* pb = &a;
	printf("%p\n", pa);
	printf("%p\n", pb);
	printf("%p\n", pa+5);
	printf("%p\n", pb+3);
	return 0;*/
	/*int arr[10];
	char* p = (char*)arr;
	int i = 0;
	for (i = 0; i < 40; i++)
	{
		*p = 'x';
		p++;
	}
	return 0;*/
	/*int* p = NULL;
	*p = 20;*/
	int arr[10] = { 0 };
	printf("%d\n", &arr[9] - &arr[0]);//指针减指针得到指针和指针之间元素的个数，前提两个指针必须指向同一块空间
	return 0;
}//sizeof(数组名)，数组名表示整个数组，计算的是整个数组的大小，&数组名，数组名表示整个数组，取出的是整个数组的地址
